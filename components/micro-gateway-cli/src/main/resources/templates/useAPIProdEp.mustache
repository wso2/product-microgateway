{{#if service.endpointConfig.prodEndpoints.endpointListEtcdEnabled}}
    {{#service.endpointConfig.prodEndpoints}}{{>endpointReinit}}{{/service.endpointConfig.prodEndpoints}}
    if (!reinitFailed) {
    clientResponse = {{service.qualifiedServiceName}}_prod->forward(urlPostfix, <@untainted>req);
    }
{{else}}
    clientResponse = {{service.qualifiedServiceName}}_prod->forward(urlPostfix, <@untainted>req);
{{/if}}
{{#service.endpointConfig.prodEndpoints}}{{>analyticsDestination}}{{/service.endpointConfig.prodEndpoints}}
